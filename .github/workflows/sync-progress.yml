name: Sync Mission Progress

on:
  push:
    branches: [main]
    paths:
      - 'config/missions.yaml'
      - 'config/waves/**'
      - 'scripts/sync_progress.py'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-mission-progress
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Generate Control Tower progress and live-status data
        run: |
          python -m pip install --disable-pip-version-check pyyaml
          python scripts/sync_progress.py
      - name: Commit generated files when changed
        run: |
          if [ -z "$(git status --porcelain -- README.md docs/03-progress/progress.md site/data/missions.json site/data/workcells.json)" ]; then
            echo "Generated Control Tower files are already synchronized."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md docs/03-progress/progress.md site/data/missions.json site/data/workcells.json
          git commit -m "chore: sync Control Tower views"
          git push
