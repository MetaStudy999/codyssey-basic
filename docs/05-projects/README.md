# 05. Projects

Mission 결과물을 장기적으로 성장시키는 **Project Lineage**를 관리한다.

## 원칙

- 새 프로젝트를 계속 만들기보다 기존 결과물을 재사용·고도화한다.
- Project의 폴더 위치는 유지하고 `growth_stage` 메타데이터만 갱신한다.
- `CORE → EXPLORE → ADVANCED → PRO`로 발전해도 폴더를 이동하지 않는다.

기계 판독용 Project Registry는 `config/projects.yaml`을 따른다.
