# B5-3 미션 용어 지도

**미션:** 로그인이 되고 회원끼리 연결되는 웹 서비스 만들기  
**분야:** 데이터베이스와 백엔드 (Database & Backend)  
**학습시간:** 60시간  
**표기 원칙:** 한글 용어 (English Term, 약어)  
**상태:** 1차 용어 목록 작성 완료 — 설명은 후속 학습 단계에서 추가

> 기준: B5-3 원본 Mission PDF의 미션 소개, 최종 결과물, 과제 목표, 기능 요구 사항, 보너스 과제, 개발 환경, 제약 사항, 결과 예시를 우선 반영한다. 원본에서 직접 요구하거나 인증·인가·연관관계·상태 변경 비즈니스 로직의 구현·검증·평가에 필요한 용어를 중심으로 구성하며, 현재 단계에서는 용어 설명을 넣지 않는다.

---

## Level 0 — 선수 용어

- 웹 애플리케이션 (Web Application)
- 클라이언트 (Client)
- 서버 (Server)
- 백엔드 (Backend)
- 파이썬 (Python)
- Python 3.10 이상 (Python 3.10+)
- FastAPI (FastAPI)
- Uvicorn (Uvicorn)
- 하이퍼텍스트 전송 프로토콜 (Hypertext Transfer Protocol, HTTP)
- 요청 (Request)
- 응답 (Response)
- 요청-응답 흐름 (Request-Response Flow)
- 경로 (Path)
- 라우트 (Route)
- 엔드포인트 (Endpoint)
- 라우터 (Router)
- 서비스 (Service)
- 저장소 / 리포지토리 (Repository)
- 모델 (Model)
- 템플릿 (Template)
- Jinja2 (Jinja2)
- 서버 사이드 렌더링 (Server-Side Rendering, SSR)
- 데이터베이스 (Database, DB)
- 관계형 데이터베이스 (Relational Database, RDB)
- SQLite (SQLite)
- PostgreSQL (PostgreSQL)
- 객체 관계 매핑 (Object-Relational Mapping, ORM)
- SQLAlchemy ORM (SQLAlchemy ORM)
- 세션 (Session)
- 의존성 (Dependency)
- 의존성 주입 (Dependency Injection, DI)
- FastAPI `Depends` (`Depends`)
- 사용자 (User)
- 계정 (Account)
- 사용자 식별자 (User Identifier, User ID)
- 비밀번호 (Password)
- 로그인 (Login / Sign-In)
- 로그아웃 (Logout / Sign-Out)
- 사용자 인터페이스 (User Interface, UI)
- 데이터 생성 (Create)
- 데이터 조회 (Read)
- 데이터 수정 (Update)
- 데이터 삭제 (Delete)
- CRUD (Create, Read, Update, Delete)
- README.md (README.md)

---

## Level 1 — 반드시 이해해야 하는 핵심 용어

### 인증과 인가

- 인증 (Authentication)
- 인가 (Authorization)
- 인증 상태 (Authentication State)
- 인증된 사용자 (Authenticated User)
- 비인증 사용자 (Unauthenticated User)
- 로그인 상태 (Logged-In State)
- 비로그인 상태 (Logged-Out State)
- 현재 사용자 (Current User)
- 접근 제어 (Access Control)
- 보호 자원 (Protected Resource)
- 공개 자원 (Public Resource)
- 공개 경로 (Public Route)
- 보호 경로 (Protected Route)
- 접근 차단 (Access Denial)
- 인증 실패 (Authentication Failure)
- 인가 실패 (Authorization Failure)
- 로그인 리다이렉트 (Login Redirect)
- 인증 의존성 (Authentication Dependency)
- 인가 의존성 (Authorization Dependency)
- 인증과 인가의 분리 (Authentication vs Authorization Separation)
- `Depends` 기반 인증 흐름 (`Depends`-Based Authentication Flow)
- 요청 사용자 식별 (Request User Identification)

### 인증 방식

- 세션 기반 인증 (Session-Based Authentication)
- 서버 세션 (Server Session)
- 세션 쿠키 (Session Cookie)
- 쿠키 (Cookie)
- JWT 기반 인증 (JWT-Based Authentication)
- JSON 웹 토큰 (JSON Web Token, JWT)
- 토큰 (Token)
- 토큰 발급 (Token Issuance)
- 토큰 검증 (Token Verification)
- 토큰 기반 인증 상태 확인 (Token-Based Authentication Check)
- 인증 방식 선택 (Authentication Strategy Selection)

### 도메인 모델과 연관관계

- 도메인 (Domain)
- 도메인 모델 (Domain Model)
- 엔터티 (Entity)
- 연관관계 (Relationship)
- 모델 관계 (Model Relationship)
- 일대다 관계 (One-to-Many Relationship, 1:N)
- 다대일 관계 (Many-to-One Relationship, N:1)
- 부모 모델 (Parent Model)
- 자식 모델 (Child Model)
- 외래 키 (Foreign Key, FK)
- 관계 매핑 (Relationship Mapping)
- 양방향 연관관계 (Bidirectional Relationship)
- SQLAlchemy `relationship`
- `back_populates`
- 관계 데이터 (Related Data)
- 관계 데이터 조회 (Related Data Retrieval)
- 객체 관점 연관관계 (Object-Oriented Relationship View)

### 비즈니스 로직

- 비즈니스 로직 (Business Logic)
- 도메인 로직 (Domain Logic)
- 상태 (State)
- 상태 변경 (State Transition)
- 상태 변경 로직 (State Transition Logic)
- 상태 변경 전 (Before State Change)
- 상태 변경 후 (After State Change)
- 비즈니스 규칙 (Business Rule)
- 핵심 비즈니스 기능 (Core Business Function)
- 단순 CRUD와 상태 변경의 차이 (CRUD vs State Transition)
- 서비스 계층 비즈니스 로직 (Service-Layer Business Logic)

### 서비스 통합

- 인증 → 도메인 → 화면 흐름 (Authentication → Domain → UI Flow)
- 로그인 → 핵심 기능 진입 → 데이터 생성/조회 → 상태 변경 → 결과 확인 흐름 (Login → Core Feature → Create/Read → State Change → Result Flow)
- 사용자 관점 흐름 (User-Centric Flow)
- 개발자 관점 흐름 (Developer-Centric Flow)
- 역할 분리 (Separation of Responsibilities)
- 계층 분리 (Layer Separation)

---

## Level 2 — 구현 필수 용어

### FastAPI 인증 구조

- 인증 모듈 (`auth/` Module)
- 인증 함수 (Authentication Function)
- 인가 함수 (Authorization Function)
- 인증 의존성 함수 (Authentication Dependency Function)
- `Depends`
- 요청 컨텍스트 (Request Context)
- 현재 사용자 의존성 (Current-User Dependency)
- 보호 라우트 (Protected Route)
- 공개 라우트 (Public Route)
- 로그인 엔드포인트 (Login Endpoint)
- 로그아웃 엔드포인트 (Logout Endpoint)
- 로그인 폼 (Login Form)
- 로그인 성공 (Login Success)
- 로그인 실패 (Login Failure)
- 로그인 실패 안내 (Login Failure Message)
- 로그아웃 처리 (Logout Processing)
- 로그인 페이지 (Login Page)
- 접근 차단 안내 화면 (Access-Denied Information Page)
- 미들웨어 (Middleware)
- 인증 미들웨어 (Authentication Middleware)
- 세션 미들웨어 (Session Middleware)

### 세션 방식

- Starlette 세션 미들웨어 (`starlette.middleware.sessions`)
- `SessionMiddleware`
- 서버 세션 정보 (Server-Side Session Data)
- 쿠키 기반 세션 식별 (Cookie-Based Session Identification)
- 세션 사용자 정보 저장 (Session User Storage)
- 세션 사용자 정보 조회 (Session User Retrieval)
- 세션 제거 (Session Clearing)
- `itsdangerous`

### JWT 방식

- JSON 웹 토큰 (JSON Web Token, JWT)
- `python-jose`
- JWT 생성 (JWT Creation)
- JWT 서명 (JWT Signing)
- JWT 검증 (JWT Verification)
- JWT 디코딩 (JWT Decoding)
- 토큰 페이로드 (Token Payload)
- 토큰 클레임 (Token Claim)
- 사용자 식별 클레임 (User Identification Claim)
- 만료 시간 (Expiration Time)
- 토큰 만료 (Token Expiration)
- 비밀키 (Secret Key)
- 토큰 전달 (Token Transport)
- 인증 헤더 (Authorization Header)
- 베어러 토큰 (Bearer Token)

### OAuth2 관련 참고 구조

- OAuth 2.0 (OAuth 2.0)
- `OAuth2PasswordBearer`
- `OAuth2PasswordRequestForm`
- 비밀번호 기반 로그인 폼 (Password Login Form)
- 액세스 토큰 (Access Token)

### 계정과 테스트 사용자

- 테스트 계정 (Test Account)
- 인메모리 테스트 계정 (In-Memory Test Account)
- 하드코딩 계정 (Hard-Coded Account)
- DB 저장 계정 (Database-Stored Account)
- 계정 ID (Account ID)
- 계정 비밀번호 (Account Password)
- 테스트 계정 정보 문서화 (Test-Account Documentation)

### 공개/보호 경로 정책

- 공개 페이지 (Public Page)
- 보호 페이지 (Protected Page)
- `/`
- `/login`
- `/signup`
- `/app/**`
- 경로 정책 (Route Policy)
- 공개/보호 경로 정책 표 (Public/Protected Route Policy Table)
- 로그인 필수 경로 (Authentication-Required Route)

### 인증 상태 기반 UI

- 조건부 UI 렌더링 (Conditional UI Rendering)
- 로그인 버튼 (Login Button)
- 로그아웃 버튼 (Logout Button)
- 사용자 환영 문구 (User Welcome Message)
- 인증 상태 표시 (Authentication Status Display)
- 핵심 기능 메뉴 활성화 (Core Feature Menu Activation)
- 핵심 기능 메뉴 비활성화 (Core Feature Menu Deactivation)

### SQLAlchemy 연관관계

- SQLAlchemy 모델 (SQLAlchemy Model)
- 최소 3개 모델 (At Least Three Models)
- 기본 키 (Primary Key, PK)
- 외래 키 (Foreign Key, FK)
- `ForeignKey`
- `relationship`
- `back_populates`
- 1:N 관계 매핑 (1:N Relationship Mapping)
- N:1 관계 매핑 (N:1 Relationship Mapping)
- 양방향 관계 매핑 (Bidirectional Relationship Mapping)
- 관계 객체 접근 (Related Object Access)
- 관계 컬렉션 (Relationship Collection)
- 관계 데이터 동시 출력 (Related-Data Rendering)
- 부모 상세 화면 (Parent Detail Page)
- 자식 목록 (Child List)
- 연관관계 기반 조회 (Relationship-Based Query)

### 부모 삭제와 자식 처리

- 삭제 정책 (Deletion Policy)
- 캐스케이드 삭제 (Cascade Delete)
- 고아 객체 삭제 (Delete Orphan)
- `cascade="all, delete-orphan"`
- 수동 삭제 (Manual Deletion)
- 부모 삭제 (Parent Deletion)
- 자식 데이터 처리 (Child Data Handling)
- 삭제 정책 선택 이유 (Deletion Policy Rationale)
- 코드 주석 (Code Comment)

### 상태 변경 중심 비즈니스 기능

- 상태 필드 (State Field)
- 상태값 (State Value)
- 상태 변경 엔드포인트 (State-Transition Endpoint)
- 상태 변경 서비스 메서드 (State-Transition Service Method)
- 상태 변경 전 검증 (Pre-Transition Validation)
- 상태 변경 실행 (State Transition Execution)
- 상태 변경 후 저장 (Post-Transition Persistence)
- 상태 변경 결과 표시 (State-Transition Result Display)
- 주문 접수 (Order Received)
- 배송 중 (Shipping)
- 배송 완료 (Delivered)
- 주문 취소 (Order Cancellation)
- 예약 대기 (Reservation Pending)
- 예약 확정 (Reservation Confirmed)
- 예약 취소 (Reservation Cancelled)
- 진행 중 (In Progress)
- 완료 (Completed)
- 공개 (Public)
- 비공개 (Private)

### 데이터 저장

- 관계형 데이터베이스 (Relational Database, RDB)
- SQLite (SQLite)
- PostgreSQL (PostgreSQL)
- SQLAlchemy ORM (SQLAlchemy ORM)
- 데이터 영속성 (Data Persistence)
- 관계 데이터 영속성 (Relationship Persistence)
- DB 기준 조회 (Database-Backed Retrieval)
- ORM 세션 (ORM Session)
- 트랜잭션 (Transaction)
- 커밋 (Commit)
- 롤백 (Rollback)

### 화면 흐름

- Jinja2 템플릿 (Jinja2 Template)
- 서버 사이드 렌더링 (Server-Side Rendering, SSR)
- 로그인 화면 (Login Screen)
- 핵심 기능 화면 (Core Feature Screen)
- 데이터 생성 화면 (Data Creation Screen)
- 데이터 조회 화면 (Data Retrieval Screen)
- 상태 변경 화면 (State-Transition Screen)
- 결과 확인 화면 (Result Screen)
- 화면 흐름 통합 (Integrated Screen Flow)

### 코드 구조

- `auth/`
- `routers/`
- `services/`
- `repositories/`
- `models/`
- `templates/`
- 인증·인가 로직 (Authentication/Authorization Logic)
- 요청/응답 처리 (Request/Response Handling)
- 비즈니스 로직 (Business Logic)
- 데이터 접근 (Data Access)
- ORM 모델 (ORM Model)
- 화면 처리 (View Rendering)
- 과도한 책임 집중 (Responsibility Concentration)
- 역할 기반 모듈 분리 (Role-Based Module Separation)

### 제출물과 README

- 전체 프로젝트 소스코드 (Full Project Source Code)
- GitHub 저장소 링크 (GitHub Repository Link)
- Python 버전 (Python Version)
- 주요 패키지 버전 (Major Package Versions)
- 가상환경 (Virtual Environment)
- 패키지 설치 (Package Installation)
- 서버 실행 방법 (Server Run Instructions)
- 테스트 계정 정보 (Test Account Information)
- ID / PW (ID / Password)
- 공개/보호 경로 정책 표 (Public/Protected Route Policy Table)
- 주요 기능 경로 목록 (Main Feature Route List)
- 인증 방식 선택 사유 (Authentication Strategy Rationale)
- 주요 화면 스크린샷 (Key Screen Screenshot)
- 시연 영상 (Demo Video)

### 핵심 패키지

- `fastapi`
- `uvicorn`
- `sqlalchemy`
- `jinja2`
- `python-multipart`
- `python-jose`
- `passlib`
- `bcrypt`
- `itsdangerous`
- `starlette`

---

## Level 3 — 구조·동작 원리 용어

### 인증·인가 흐름

- 인증 요청 흐름 (Authentication Request Flow)
- 로그인 요청 (Login Request)
- 자격 증명 (Credential)
- 사용자 식별 (User Identification)
- 인증 검증 (Authentication Verification)
- 인증 상태 생성 (Authentication State Creation)
- 인증 상태 유지 (Authentication State Persistence)
- 후속 요청 (Subsequent Request)
- 현재 사용자 복원 (Current-User Restoration)
- 보호 경로 검사 (Protected-Route Check)
- 인증 실패 차단 (Unauthenticated Request Blocking)
- 인증 → 인가 순서 (Authentication → Authorization Order)
- 인증되지 않은 요청 차단 지점 (Unauthenticated Request Blocking Point)
- 의존성 체인 (Dependency Chain)
- `Depends` 실행 흐름 (`Depends` Execution Flow)
- 미들웨어 실행 흐름 (Middleware Execution Flow)
- 미들웨어와 `Depends`의 역할 구분 (Middleware vs `Depends` Responsibility)

### 세션과 JWT 비교 구조

- 서버 상태 기반 인증 (Server-Stateful Authentication)
- 토큰 기반 인증 (Token-Based Authentication)
- 상태 저장 인증 (Stateful Authentication)
- 상태 비저장 인증 (Stateless Authentication)
- 세션 식별자 (Session Identifier)
- 쿠키 전달 (Cookie Transport)
- JWT 서명 검증 (JWT Signature Verification)
- 인증 상태 저장 위치 (Authentication State Storage Location)
- 인증 방식 비교 (Session vs JWT Comparison)

### SQLAlchemy 연관관계 구조

- 관계형 모델 (Relational Model)
- 객체 모델 (Object Model)
- 객체-테이블 매핑 (Object-Table Mapping)
- 객체 간 연관관계 (Object Relationship)
- FK 기반 관계 (Foreign-Key-Based Relationship)
- `relationship` 기반 객체 탐색 (Object Navigation via `relationship`)
- `back_populates` 기반 양방향 연결 (Bidirectional Link via `back_populates`)
- 부모 → 자식 탐색 (Parent → Child Navigation)
- 자식 → 부모 탐색 (Child → Parent Navigation)
- 관계 로딩 (Relationship Loading)
- 관계 데이터 렌더링 (Relationship Data Rendering)
- 삭제 전파 (Delete Propagation)
- 삭제 생명주기 (Deletion Lifecycle)
- 캐스케이드 정책 (Cascade Policy)

### 상태 변경 로직 구조

- 상태 머신 개념 (State Machine Concept)
- 상태 전이 (State Transition)
- 현재 상태 (Current State)
- 목표 상태 (Target State)
- 허용 상태 전이 (Allowed State Transition)
- 비허용 상태 전이 (Invalid State Transition)
- 상태 전이 조건 (Transition Condition)
- 상태 전이 규칙 (Transition Rule)
- 상태 변경 검증 (Transition Validation)
- 상태 변경 저장 (Transition Persistence)
- 상태 변경 후 화면 반영 (Post-Transition UI Update)
- 서비스 계층 상태 변경 (Service-Layer State Transition)
- CRUD 중심 구조 (CRUD-Centric Structure)
- 비즈니스 상태 중심 구조 (Business-State-Centric Structure)

### 전체 서비스 구조

- 인증 계층 (Authentication Layer)
- 인가 계층 (Authorization Layer)
- 라우팅 계층 (Routing Layer)
- 서비스 계층 (Service Layer)
- 데이터 접근 계층 (Data Access Layer)
- 모델 계층 (Model Layer)
- 표현 계층 (Presentation Layer)
- 요청 → 인증 → 인가 → 라우터 → 서비스 → 저장소 → DB → 템플릿 → 응답 흐름 (Request → Authentication → Authorization → Router → Service → Repository → DB → Template → Response Flow)
- 인증 → 도메인 → 화면 통합 (Authentication → Domain → UI Integration)
- 사용자 흐름 (User Flow)
- 개발자 구조 (Developer Structure)

---

## Level 4 — 검증·트러블슈팅 용어

> 이 단계는 원본 Mission PDF가 요구하는 동작 검증 항목과 해당 항목을 확인하기 위해 직접 필요한 오류·검증 용어를 중심으로 구성한다.

### 인증 검증

- 로그인 성공 검증 (Login Success Verification)
- 로그인 실패 검증 (Login Failure Verification)
- 로그아웃 검증 (Logout Verification)
- 로그인 상태 유지 확인 (Authentication Persistence Verification)
- 세션 저장 확인 (Session Storage Verification)
- 세션 제거 확인 (Session Clearing Verification)
- JWT 발급 확인 (JWT Issuance Verification)
- JWT 검증 확인 (JWT Verification Check)
- 토큰 만료 확인 (Token Expiration Check)
- 잘못된 토큰 (Invalid Token)
- 누락된 토큰 (Missing Token)
- 잘못된 자격 증명 (Invalid Credential)
- 인증 실패 원인 확인 (Authentication Failure Diagnosis)

### 인가 검증

- 비로그인 접근 차단 검증 (Unauthenticated Access Blocking Verification)
- 보호 경로 접근 차단 (Protected-Route Access Blocking)
- 공개 경로 접근 확인 (Public-Route Access Verification)
- 로그인 페이지 리다이렉트 확인 (Login Redirect Verification)
- 접근 차단 안내 확인 (Access-Denied Message Verification)
- 공개/보호 경로 정책 검증 (Public/Protected Route Policy Verification)

### UI 검증

- 로그인 전 UI 검증 (Logged-Out UI Verification)
- 로그인 후 UI 검증 (Logged-In UI Verification)
- 로그인 버튼 표시 (Login Button Visibility)
- 로그아웃 버튼 표시 (Logout Button Visibility)
- 환영 문구 표시 (Welcome Message Visibility)
- 핵심 메뉴 활성화 확인 (Core Menu Activation Verification)

### 연관관계 검증

- 모델 수 검증 (Model Count Verification)
- 1:N 관계 검증 (1:N Relationship Verification)
- N:1 관계 검증 (N:1 Relationship Verification)
- 양방향 연관관계 검증 (Bidirectional Relationship Verification)
- `relationship` 설정 확인 (`relationship` Configuration Check)
- `back_populates` 설정 확인 (`back_populates` Configuration Check)
- FK 연결 확인 (Foreign-Key Link Verification)
- 관계 데이터 조회 검증 (Related-Data Retrieval Verification)
- 관계 데이터 화면 출력 검증 (Related-Data Rendering Verification)
- 부모 삭제 후 자식 처리 검증 (Child Handling After Parent Deletion)
- 캐스케이드 삭제 검증 (Cascade Delete Verification)
- 수동 삭제 검증 (Manual Delete Verification)
- 고아 데이터 (Orphan Data)
- 무결성 오류 (Integrity Error)

### 상태 변경 검증

- 상태 변경 기능 검증 (State-Transition Feature Verification)
- 상태 변경 전/후 비교 (Before/After State Comparison)
- 상태 변경 결과 저장 확인 (State Persistence Verification)
- 상태 변경 결과 화면 확인 (State Result UI Verification)
- 비허용 상태 변경 차단 (Invalid Transition Blocking)
- 중복 상태 변경 (Duplicate Transition)
- 상태 불일치 (State Mismatch)

### 서비스 전체 흐름 검증

- 로그인 → 핵심 기능 → 데이터 생성/조회 → 상태 변경 → 결과 확인 전체 흐름 검증 (End-to-End Service Flow Verification)
- 끊김 없는 사용자 흐름 (Continuous User Flow)
- 데이터베이스 영속성 검증 (Database Persistence Verification)
- 관계 데이터 DB 조회 검증 (Database-Backed Relationship Verification)
- 실행 절차 재현 (Run-Procedure Reproduction)
- README 재현성 (README Reproducibility)

### 오류 상황

- HTTP 401 Unauthorized (401 Unauthorized)
- HTTP 403 Forbidden (403 Forbidden)
- 인증 예외 (Authentication Exception)
- 접근 거부 (Access Denied)
- 세션 누락 (Missing Session)
- 쿠키 누락 (Missing Cookie)
- 토큰 검증 실패 (Token Verification Failure)
- 외래 키 오류 (Foreign Key Error)
- 관계 매핑 오류 (Relationship Mapping Error)
- 순환 참조 개념 (Circular Reference Concept)
- 삭제 정책 오류 (Deletion Policy Error)
- 상태 전이 오류 (State Transition Error)
- 없는 데이터 요청 (Missing-Data Request)
- 잘못된 접근 (Invalid Access)

---

## Level 5 — 평가에서 설명해야 하는 용어

### 인증·인가 설명

- 인증과 인가의 차이 (Authentication vs Authorization)
- FastAPI 인증 흐름 설명 (Explaining FastAPI Authentication Flow)
- `Depends` 기반 인증 처리 설명 (Explaining `Depends`-Based Authentication)
- 인증되지 않은 요청이 차단되는 이유 (Why Unauthenticated Requests Are Blocked)
- 요청이 차단되는 위치 (Where Unauthenticated Requests Are Blocked)
- 인증 방식 선택 이유 (Session/JWT Strategy Rationale)
- 세션 방식 설명 (Explaining Session-Based Authentication)
- JWT 방식 설명 (Explaining JWT-Based Authentication)
- 공개/보호 경로 정책 설명 (Explaining Public/Protected Route Policy)
- 인증 상태 기반 UI 변화 설명 (Explaining Auth-State-Based UI Change)

### 모델과 관계 설명

- SQLAlchemy 연관관계 설명 (Explaining SQLAlchemy Relationships)
- 1:N 관계 설명 (Explaining One-to-Many Relationship)
- N:1 관계 설명 (Explaining Many-to-One Relationship)
- `relationship` 역할 (Role of `relationship`)
- `back_populates` 역할 (Role of `back_populates`)
- 양방향 연관관계 선택 이유 (Why Bidirectional Relationship Is Used)
- 객체 관점 관계 이해 (Object-Oriented Relationship Understanding)
- 관계 매핑 이유 (Relationship Mapping Rationale)
- 부모 삭제 정책 선택 이유 (Parent-Deletion Policy Rationale)
- 캐스케이드와 수동 삭제 선택 이유 (Cascade vs Manual Delete Rationale)

### 비즈니스 로직 설명

- 단순 CRUD와 상태 변경 로직 차이 (CRUD vs State-Transition Logic)
- 상태 변경 로직이 중요한 이유 (Why State-Transition Logic Matters)
- 상태 변경 로직을 서비스 계층에 두는 이유 (Why State-Transition Logic Belongs in Service Layer)
- 상태 변경 전/후 검증 설명 (Explaining Before/After State Verification)
- 비즈니스 규칙 설명 (Explaining Business Rules)

### 전체 구조 설명

- 인증 → 도메인 → 화면 구조 설명 (Explaining Authentication → Domain → UI Structure)
- 사용자 관점 서비스 흐름 설명 (Explaining User-Centric Service Flow)
- 개발자 관점 계층 구조 설명 (Explaining Developer-Centric Layer Structure)
- `auth/`, `routers/`, `services/`, `repositories/`, `models/`, `templates/` 역할 설명 (Explaining Module Responsibilities)
- 역할 분리 이유 (Why Responsibilities Are Separated)
- 데이터 접근과 비즈니스 로직 분리 이유 (Why Data Access and Business Logic Are Separated)
- 관계형 DB를 사용하는 이유 (Why a Relational Database Is Used)
- 관계 데이터를 DB 기준으로 조회하는 이유 (Why Related Data Is Retrieved from DB)
- 전체 서비스 통합 완성도 설명 (Explaining Service Integration Completeness)

---

## Advanced — 고도화 용어

> 원본 Mission PDF의 보너스 과제와 후속 학습 연결에 직접 등장하는 범위만 포함한다.

### 전역 예외 처리

- 전역 예외 처리 (Global Exception Handling)
- FastAPI `@app.exception_handler`
- 커스텀 예외 클래스 (Custom Exception Class)
- 일관된 오류 응답 (Consistent Error Response)
- 실패 상황 공통 처리 (Centralized Failure Handling)

### 검색 / 필터

- 검색 (Search)
- 필터링 (Filtering)
- 쿼리 파라미터 (Query Parameter)
- 조건 검색 (Conditional Search)
- 동적 쿼리 (Dynamic Query)
- SQLAlchemy 동적 쿼리 구성 (Dynamic SQLAlchemy Query Construction)

### OAuth2 소셜 로그인

- 소셜 로그인 (Social Login)
- OAuth 2.0 인증 (OAuth 2.0 Authentication)
- OAuth2 Authorization Code Flow (OAuth2 Authorization Code Flow)
- 권한 부여 코드 (Authorization Code)
- 외부 아이덴티티 제공자 (External Identity Provider, IdP)
- Google 로그인 (Google Login)
- GitHub 로그인 (GitHub Login)
- 외부 IdP 연동 (External IdP Integration)

### 회원가입과 비밀번호 보안

- 회원가입 (Sign-Up / Registration)
- 비밀번호 해싱 (Password Hashing)
- 해시 (Hash)
- 솔트 (Salt)
- `passlib`
- `bcrypt`
- 비밀번호 해시 저장 (Password Hash Storage)
- 비밀번호 검증 (Password Verification)

### 외부 배포

- 외부 배포 (External Deployment)
- Render (Render)
- Railway (Railway)
- 클라우드 환경 (Cloud Environment)
- 외부 접속 URL (Public URL)
- 환경 변수 (Environment Variable)
- 비밀정보 환경 변수 관리 (Secret Environment Variable Management)

---

## Top Core — B5-3 시작 전에 반드시 잡을 용어

1. 인증 (Authentication)
2. 인가 (Authorization)
3. FastAPI `Depends`
4. 의존성 주입 (Dependency Injection, DI)
5. 현재 사용자 (Current User)
6. 공개 경로 (Public Route)
7. 보호 경로 (Protected Route)
8. 접근 제어 (Access Control)
9. 세션 기반 인증 (Session-Based Authentication)
10. JWT 기반 인증 (JWT-Based Authentication)
11. 쿠키 (Cookie)
12. JSON 웹 토큰 (JSON Web Token, JWT)
13. 로그인 (Login)
14. 로그아웃 (Logout)
15. 인증 상태 기반 UI (Auth-State-Based UI)
16. SQLAlchemy ORM (SQLAlchemy ORM)
17. 도메인 모델 (Domain Model)
18. 일대다 관계 (One-to-Many Relationship, 1:N)
19. 다대일 관계 (Many-to-One Relationship, N:1)
20. `ForeignKey`
21. `relationship`
22. `back_populates`
23. 양방향 연관관계 (Bidirectional Relationship)
24. 부모-자식 관계 (Parent-Child Relationship)
25. 캐스케이드 삭제 (Cascade Delete)
26. 상태 변경 (State Transition)
27. 비즈니스 로직 (Business Logic)
28. 서비스 계층 (Service Layer)
29. 인증 → 도메인 → 화면 흐름 (Authentication → Domain → UI Flow)
30. 로그인 → 핵심 기능 → 데이터 생성/조회 → 상태 변경 → 결과 확인 흐름 (Login → Core Feature → Create/Read → State Change → Result Flow)

---

## 중요도 적용

### CORE

- Authentication / Authorization
- Depends / Dependency Injection
- Current User
- Public Route / Protected Route
- Access Control
- Session-Based Authentication / JWT-Based Authentication
- Login / Logout
- Auth-State-Based UI
- SQLAlchemy ORM
- 1:N / N:1
- ForeignKey / relationship / back_populates
- Bidirectional Relationship
- State Transition
- Business Logic
- Service Layer
- Authentication → Domain → UI Flow

### REQUIRED

- Session / Cookie / JWT / Token Verification
- OAuth2PasswordBearer / OAuth2PasswordRequestForm
- auth/ / routers/ / services/ / repositories/ / models/ / templates/
- Test Account
- Public/Protected Route Policy Table
- Relationship Data Rendering
- Cascade Delete / delete-orphan / Manual Delete
- SQLite / PostgreSQL
- Jinja2 SSR
- README Reproducibility

### REFERENCE

- Global Exception Handling
- Search / Filtering
- OAuth2 Social Login
- Authorization Code Flow / IdP
- Sign-Up / Password Hashing / passlib / bcrypt
- Render / Railway / Environment Variables

---

## 20 : 60 : 20 학습 배치

### 시작 전 20%

- Authentication / Authorization
- Depends / Dependency Injection
- Current User
- Public Route / Protected Route
- Session vs JWT
- Login / Logout
- 1:N / N:1
- ForeignKey / relationship / back_populates
- State Transition

### 구현 중 60%

- auth/ 인증 모듈
- 세션 또는 JWT 로그인 흐름
- 공개/보호 경로 정책
- 인증 상태 기반 UI
- 최소 3개 ORM 모델
- 최소 2개 1:N 또는 N:1 관계
- 양방향 관계
- 관계 데이터 출력
- 부모 삭제 정책
- 상태 변경 서비스 로직
- DB 영속성
- Jinja2 SSR 화면 흐름
- 계층별 코드 분리
- README 제출 요건

### 완료 후 20%

- 인증/인가 차이 자기 설명
- 비로그인 요청 차단 지점 설명
- 세션/JWT 선택 이유 설명
- SQLAlchemy 관계 매핑 이유 설명
- 삭제 정책 선택 이유 설명
- 상태 변경 로직의 위치와 이유 설명
- 사용자 관점 전체 흐름 설명
- 개발자 관점 전체 구조 설명

---

## 누적 학습 연결

### B5-2 → B5-3

- REVIEW: FastAPI / HTTP / Router / Service / Repository / Model / Template
- REVIEW: Jinja2 / SSR / Form / Session / Depends / Dependency Injection
- REVIEW: SQLite / SQLAlchemy ORM / CRUD
- DEEPEN: 단일 모델 → 최소 3개 연관 모델
- DEEPEN: 인증 없는 CRUD → 인증·인가 통합 서비스
- DEEPEN: 단순 CRUD → 상태 변경 중심 비즈니스 로직
- APPLY: Router → Service → Repository → DB → Template 흐름에 Authentication / Authorization 삽입

### B5-3 → B6-1

- APPLY: FastAPI 웹 서비스 외부 배포
- APPLY: 환경 변수 기반 비밀정보 관리
- APPLY: 외부 접속 URL과 서버 운영
- DEEPEN: 인증된 웹 애플리케이션의 운영 환경 구성

---

## STOP RULE — 이번 미션에서 제외하거나 선택으로 남기는 범위

- 다대다 관계 (Many-to-Many Relationship, N:M) — 필수 범위 아님
- 복잡한 역할 기반 접근 제어 (Role-Based Access Control, RBAC) — 로그인/비로그인 구분만으로 충분
- 다단계 권한 체계 (Multi-Level Authorization) — 필수 범위 아님
- OAuth2 소셜 로그인 (OAuth2 Social Login) — 보너스 과제
- 회원가입과 비밀번호 해싱 (Sign-Up & Password Hashing) — 보너스 과제
- 외부 배포 (External Deployment) — 보너스 과제
- 전역 예외 처리 (Global Exception Handling) — 보너스 과제
- 검색 / 필터 (Search / Filtering) — 보너스 과제

---

## 다음 학습 단계

이 문서는 **용어 지도 1차 단계**다. 후속 단계에서는 Top Core부터 다음 순서로 확장한다.

`쉬운 한 줄 설명 → 비유 → 관계도 → 최소 예제 → 실제 미션 위치 → 오류 재현 → 수정 → 평가 질문 → 자기 설명`
