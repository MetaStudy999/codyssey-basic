# B3-2 미션 용어 지도

**미션:** 파일이 언제 어떻게 바뀌었는지 기록하는 작은 프로그램 만들기  
**분야:** 자료구조와 알고리즘 (Data Structures & Algorithms)  
**학습시간:** 80시간  
**표기 원칙:** 한글 용어 (English Term, 약어)  
**상태:** 1차 용어 목록 작성 완료 — 설명은 후속 학습 단계에서 추가

> 기준: B3-2 원본 Mission PDF의 미션 소개, 최종 결과물, 과제 목표, 기능 요구 사항, 보너스 과제, 개발 환경, 제약 사항, 결과 예시를 우선 반영한다. 원본에서 직접 요구하거나 구현·검증·평가에 필요한 용어를 중심으로 구성하며, 현재 단계에서는 용어 설명을 넣지 않는다.

---

## Level 0 — 선수 용어

- 자료구조 (Data Structure)
- 알고리즘 (Algorithm)
- 파이썬 (Python)
- 파이썬 3.10 이상 (Python 3.10+)
- 프로그램 (Program)
- 명령줄 인터페이스 (Command-Line Interface, CLI)
- 읽기-평가-출력 반복문 (Read-Eval-Print Loop, REPL)
- 명령어 (Command)
- 명령어 파싱 (Command Parsing)
- 입력 (Input)
- 출력 (Output)
- 문자열 (String)
- 정수 (Integer)
- 리스트 (List)
- 딕셔너리 (Dictionary)
- 집합 (Set)
- 튜플 (Tuple)
- 클래스 (Class)
- 객체 (Object)
- 함수 (Function)
- 메서드 (Method)
- 변수 (Variable)
- 조건문 (Conditional Statement)
- 반복문 (Loop)
- 재귀 (Recursion)
- 해시 (Hash)
- 해시맵 (Hash Map)
- 키-값 쌍 (Key-Value Pair)
- 노드 (Node)
- 간선 (Edge)
- 그래프 (Graph)
- 경로 (Path)
- 탐색 (Traversal / Search)
- 정렬 (Sorting)
- 시간복잡도 (Time Complexity)
- 공간복잡도 (Space Complexity)
- 빅오 표기법 (Big-O Notation)
- Git
- 분산 버전 관리 시스템 (Distributed Version Control System, DVCS)
- 저장소 (Repository)
- 커밋 (Commit)
- 브랜치 (Branch)
- HEAD
- 작성자 (Author)
- 타임스탬프 (Timestamp)

---

## Level 1 — 반드시 이해해야 하는 핵심 용어

- 미니 Git (Mini Git)
- 커밋 그래프 (Commit Graph)
- 방향 그래프 (Directed Graph)
- 비순환 그래프 (Acyclic Graph)
- 방향성 비순환 그래프 (Directed Acyclic Graph, DAG)
- 커밋 노드 (Commit Node)
- 부모 커밋 (Parent Commit)
- 다중 부모 커밋 (Multi-Parent Commit)
- 루트 커밋 (Root Commit)
- 커밋 해시 (Commit Hash)
- 해시 유일성 (Hash Uniqueness)
- 브랜치 포인터 (Branch Pointer)
- 현재 브랜치 (Current Branch)
- HEAD 포인터 (HEAD Pointer)
- 조상 커밋 (Ancestor Commit)
- 조상 탐색 (Ancestor Traversal)
- 최단 경로 (Shortest Path)
- 무방향 간선 (Undirected Edge)
- 그래프 탐색 (Graph Traversal)
- 너비 우선 탐색 (Breadth-First Search, BFS)
- 위상 정렬 (Topological Sort)
- 위상 정렬 성격의 출력 (Topological-Order-Like Output)
- 부모 우선 출력 (Parent-Before-Child Order)
- 역색인 (Inverted Index)
- 토큰 (Token)
- 토큰화 (Tokenization)
- 소문자 정규화 (Lowercase Normalization)
- 키워드 인덱스 (Keyword Index)
- 작성자 인덱스 (Author Index)
- 직접 구현 정렬 (Manual Sorting Implementation)
- 비교 기준 (Comparison Key / Criterion)
- 안정 정렬 (Stable Sort)
- 불안정 정렬 (Unstable Sort)
- 평균 시간복잡도 (Average Time Complexity)
- 최악 시간복잡도 (Worst-Case Time Complexity)

---

## Level 2 — 구현 필수 용어

### 저장소·브랜치·커밋

- 저장소 초기화 (Repository Initialization)
- `INIT <user_name>`
- 사용자 이름 (User Name)
- 현재 사용자 (Current User)
- 메인 브랜치 (Main Branch)
- `BRANCH <branch_name>`
- 브랜치 이름 (Branch Name)
- 브랜치 생성 (Branch Creation)
- `SWITCH <branch_name>`
- 브랜치 전환 (Branch Switching)
- `COMMIT <message>`
- 커밋 메시지 (Commit Message)
- 커밋 생성 (Commit Creation)
- 커밋 메타데이터 (Commit Metadata)
- `hash`
- `message`
- `author`
- `timestamp`
- `parents`
- 증가 카운터 기반 해시 (Incrementing-Counter-Based Hash)
- 난수 기반 해시 (Random-Based Hash)
- 중복 방지 (Duplicate Prevention)

### 로그·탐색

- `LOG`
- 커밋 로그 (Commit Log)
- 로그 출력 (Log Output)
- `PATH <commit1> <commit2>`
- 경로 탐색 (Path Search)
- 간선 수 (Edge Count)
- 최소 간선 수 (Minimum Edge Count)
- 연결 관계 (Connectivity)
- 경로 없음 (No Path)
- `No path`
- 다중 최단 경로 (Multiple Shortest Paths)
- 사전순 (Lexicographic Order)
- 경로 문자열 (Path String)
- `hash1->hash2->...`
- `ANCESTORS <commit_hash>`
- 모든 조상 (All Ancestors)
- 도달 가능성 (Reachability)
- 방문 집합 (Visited Set)

### 검색·역색인

- `SEARCH <keyword>`
- 키워드 검색 (Keyword Search)
- `SEARCH --author=<name>`
- 작성자 검색 (Author Search)
- 공백 기준 분리 (Whitespace Split)
- `split()`
- `lower()`
- 키워드 추출 (Keyword Extraction)
- `keyword -> commit_hash 목록`
- `author -> commit_hash 목록`
- 후보 커밋 (Candidate Commit)
- 전체 순회 검색 (Full Scan)
- 인덱스 조회 (Index Lookup)
- 인덱스 갱신 (Index Update)

### 정렬

- `LOG --sort-by=date|author`
- 날짜 기준 정렬 (Sort by Date)
- 작성자 기준 정렬 (Sort by Author)
- 동률 처리 (Tie Handling)
- 비교 함수 (Comparison Function)
- 선택 정렬 (Selection Sort)
- 삽입 정렬 (Insertion Sort)
- 버블 정렬 (Bubble Sort)
- 병합 정렬 (Merge Sort)
- 퀵 정렬 (Quick Sort)
- 표준 정렬 API (Standard Sorting API)
- `sorted()`
- `list.sort()`

### CLI 문법

- 대소문자 비구분 명령어 (Case-Insensitive Command)
- 문자열 인자 (String Argument)
- 공백 포함 문자열 (Space-Containing String)
- 따옴표 인자 (Quoted Argument)
- 옵션 (Option)
- `--author=<name>`
- `--sort-by=date|author`
- 알 수 없는 명령 (Unknown Command)
- 잘못된 인자 (Invalid Arguments)
- `Invalid args`
- 알 수 없는 브랜치 (Unknown Branch)
- `Unknown branch: <name>`
- 알 수 없는 커밋 (Unknown Commit)
- `Unknown commit: <hash>`
- 프롬프트 (Prompt)
- `mini-git>`
- `exit`
- `quit`
- 엔트리 포인트 (Entry Point)
- `main.py`
- `README.md`
- `python main.py`

---

## Level 3 — 구조·동작 원리 용어

- DAG 불변조건 (DAG Invariant)
- 순환 (Cycle)
- 순환 방지 (Cycle Prevention)
- 부모-자식 관계 (Parent-Child Relationship)
- 커밋 저장소 (Commit Store)
- 해시 기반 커밋 조회 (Hash-Based Commit Lookup)
- 브랜치-커밋 매핑 (Branch-to-Commit Mapping)
- HEAD-브랜치 관계 (HEAD-to-Branch Relationship)
- 현재 HEAD 커밋 (Current HEAD Commit)
- 브랜치 분기 (Branch Divergence)
- 병렬 작업 (Parallel Work)
- 변경 이력 (Change History)
- 그래프 표현 (Graph Representation)
- 인접 리스트 (Adjacency List)
- 부모 목록 (Parent List)
- 자식 관계 유도 (Derived Child Relationship)
- 큐 (Queue)
- BFS 큐 (BFS Queue)
- 거리 (Distance)
- 이전 노드 / 부모 추적 정보 (Predecessor Tracking)
- 경로 복원 (Path Reconstruction)
- 사전순 타이브레이크 (Lexicographic Tie-Breaking)
- 깊이 우선 탐색 (Depth-First Search, DFS)
- 조상 집합 (Ancestor Set)
- 중복 방문 방지 (Visited Deduplication)
- 진입차수 (In-Degree)
- 위상 순서 (Topological Order)
- 인덱싱 (Indexing)
- 검색 비용 (Search Cost)
- 순회 비용 (Scan Cost)
- 정렬 비교 연산 (Sorting Comparison)
- 정렬 교환 연산 (Swap Operation)
- 정렬 안정성 (Sort Stability)
- 결정적 출력 (Deterministic Output)
- 세션 (Session)
- 세션 내 유일성 (Session-Local Uniqueness)
- 메모리 상 저장 (In-Memory Storage)

---

## Level 4 — 검증·트러블슈팅 용어

- 기능 검증 (Functional Verification)
- 입력 검증 (Input Validation)
- 인자 개수 검증 (Argument Count Validation)
- 옵션 검증 (Option Validation)
- 존재하지 않는 브랜치 (Nonexistent Branch)
- 존재하지 않는 커밋 (Nonexistent Commit)
- 중복 브랜치 (Duplicate Branch)
- 중복 커밋 해시 (Duplicate Commit Hash)
- 빈 저장소 (Empty Repository)
- 초기 커밋 없음 (No Initial Commit)
- 부모 없는 커밋 (Parentless Commit)
- 연결되지 않은 커밋 그래프 (Disconnected Commit Graph)
- 경로 없음 검증 (No-Path Verification)
- 다중 최단 경로 검증 (Multiple-Shortest-Path Verification)
- 사전순 선택 검증 (Lexicographic Selection Verification)
- 조상 누락 (Missing Ancestor)
- 중복 조상 출력 (Duplicate Ancestor Output)
- 순환 탐지 (Cycle Detection)
- 부모 우선 순서 검증 (Parent-Before-Child Verification)
- 역색인 일관성 (Inverted Index Consistency)
- 키워드 정규화 검증 (Keyword Normalization Verification)
- 작성자 인덱스 검증 (Author Index Verification)
- 전체 순회 없는 검색 (Search Without Full Scan)
- 정렬 결과 검증 (Sort Result Verification)
- 날짜 동률 (Date Tie)
- 작성자 동률 (Author Tie)
- 빈 검색 결과 (Empty Search Result)
- 잘못된 명령어 형식 (Malformed Command)
- 따옴표 파싱 오류 (Quoted-Argument Parsing Error)
- 케이스 비구분 검증 (Case-Insensitive Parsing Verification)
- 예외 처리 (Exception Handling)
- 에러 메시지 표준화 (Error Message Standardization)
- 단위 테스트 (Unit Test)
- 경계값 테스트 (Boundary Test)
- 테스트 케이스 (Test Case)

---

## Level 5 — 평가에서 설명해야 하는 용어

- Git 커밋 구조 (Git Commit Structure)
- 커밋 그래프가 DAG인 이유 (Why the Commit Graph Is a DAG)
- 부모가 먼저 출력되는 로그 (Parent-Before-Child Log)
- 위상 정렬 성격의 출력 (Topological-Order-Like Output)
- 최단 경로 탐색 (Shortest-Path Search)
- BFS 기반 최단 경로 (BFS-Based Shortest Path)
- 무방향으로 간주한 커밋-부모 연결 (Commit-Parent Links Treated as Undirected)
- 모든 조상 탐색 (All-Ancestor Traversal)
- 정렬 알고리즘 직접 구현 (Manual Sorting Algorithm Implementation)
- 평균 시간복잡도 (Average Time Complexity)
- 최악 시간복잡도 (Worst-Case Time Complexity)
- 안정 정렬 여부 (Sort Stability)
- 역색인 동작 원리 (Inverted Index Mechanism)
- 역색인과 순회 검색 비교 (Inverted Index vs Full Scan)
- 검색 시간복잡도 (Search Time Complexity)
- 브랜치가 현재 커밋을 가리키는 구조 (Branch Pointing to Current Commit)
- HEAD 전환 (HEAD Switching)
- 커밋 생성 시 인덱스 갱신 (Index Update on Commit)
- 해시맵 기반 커밋 조회 (Hash-Map-Based Commit Lookup)
- 커밋 해시 유일성 (Commit Hash Uniqueness)
- 알고리즘 로직 분리 (Algorithm Logic Separation)
- 함수 / 클래스 분리 (Function / Class Separation)
- 주석 (Comment)
- 독스트링 (Docstring)

---

## Advanced — 고도화 용어

### Diff 보너스

- 차이 비교 (Diff)
- `diff <file1> <file2>`
- 텍스트 파일 비교 (Text File Comparison)
- 줄 단위 비교 (Line-by-Line Comparison)
- 추가 줄 (Added Line)
- 삭제 줄 (Deleted Line)
- 공통 줄 (Common Line)

### Merge 보너스

- 병합 (Merge)
- `merge <branch_name>`
- 병합 커밋 (Merge Commit)
- 두 부모 커밋 (Two-Parent Commit)
- 현재 브랜치 HEAD (Current Branch HEAD)
- 대상 브랜치 HEAD (Target Branch HEAD)

### 정렬 성능 비교 보너스

- 정렬 알고리즘 성능 비교 (Sorting Algorithm Performance Comparison)
- 실행 시간 (Execution Time)
- 입력 크기 (Input Size)
- 벤치마크 (Benchmark)
- 성능 측정 (Performance Measurement)

### 실제 Git 연결 개념

- 리베이스 (Rebase)
- 체리픽 (Cherry-Pick)
- 머지 (Merge)

---

## Top Core — 미션 시작 전에 반드시 이해해야 하는 용어

- 그래프 (Graph)
- 노드 (Node)
- 간선 (Edge)
- 방향성 비순환 그래프 (Directed Acyclic Graph, DAG)
- 커밋 (Commit)
- 커밋 그래프 (Commit Graph)
- 부모 커밋 (Parent Commit)
- 커밋 해시 (Commit Hash)
- 해시맵 (Hash Map)
- 브랜치 (Branch)
- HEAD
- 브랜치 포인터 (Branch Pointer)
- 조상 커밋 (Ancestor Commit)
- 그래프 탐색 (Graph Traversal)
- 너비 우선 탐색 (Breadth-First Search, BFS)
- 최단 경로 (Shortest Path)
- 위상 정렬 (Topological Sort)
- 부모 우선 출력 (Parent-Before-Child Order)
- 역색인 (Inverted Index)
- 토큰화 (Tokenization)
- 키워드 인덱스 (Keyword Index)
- 작성자 인덱스 (Author Index)
- 정렬 알고리즘 (Sorting Algorithm)
- 시간복잡도 (Time Complexity)
- 안정 정렬 (Stable Sort)
- 명령줄 인터페이스 (Command-Line Interface, CLI)
- 읽기-평가-출력 반복문 (Read-Eval-Print Loop, REPL)

---

## 20 : 60 : 20 학습 배치

### 미션 전 20%

- Level 0의 그래프·Git·복잡도 선수 용어
- Level 1의 DAG·커밋 그래프·BFS·위상 정렬·역색인·정렬 핵심 용어
- Top Core

### 미션 수행 중 60%

- INIT / BRANCH / SWITCH / COMMIT
- LOG / PATH / ANCESTORS
- SEARCH / `--author`
- `LOG --sort-by=date|author`
- 커밋 저장소·브랜치·HEAD·역색인 구현
- BFS 최단 경로·조상 탐색·부모 우선 로그 구현
- 직접 정렬 알고리즘 구현
- CLI 파싱·에러 처리

### 미션 완료 후 20%

- DAG 설명
- BFS·조상 탐색 설명
- 위상 정렬 성격의 로그 설명
- 정렬 시간복잡도와 안정성 설명
- 역색인과 전체 순회 검색 비교
- 보너스 Diff / Merge / 정렬 성능 비교

---

## 누적 학습 연결

### B2-2 → B3-2 REVIEW / APPLY

- Git
- 저장소 (Repository)
- 커밋 (Commit)
- 브랜치 (Branch)
- HEAD
- Merge
- Rebase
- Cherry-Pick

### B3-1 → B3-2 REVIEW / DEEPEN

- 자료구조 (Data Structure)
- 알고리즘 (Algorithm)
- 해시맵 (Hash Map)
- 키-값 쌍 (Key-Value Pair)
- 시간복잡도 (Time Complexity)
- 직접 자료구조·알고리즘 구현 (Direct Data Structure / Algorithm Implementation)
- CLI / REPL

### B3-2에서 NEW / DEEPEN

- 방향성 비순환 그래프 (Directed Acyclic Graph, DAG)
- 커밋 그래프 (Commit Graph)
- 너비 우선 탐색 (Breadth-First Search, BFS)
- 최단 경로 (Shortest Path)
- 조상 탐색 (Ancestor Traversal)
- 위상 정렬 (Topological Sort)
- 역색인 (Inverted Index)
- 직접 구현 정렬 (Manual Sorting Implementation)
- 정렬 안정성 (Sort Stability)

---

## 원본 미션의 명시적 범위 밖

- 파일 내용 추적 (File Content Tracking)
- 네트워크 통신 (Network Communication)
- 데이터 영속성 / 파일 저장 (Data Persistence / File Storage)
- 그래프 전용 라이브러리 (Graph-Specific Library)
- Python 표준 정렬 API (Python Standard Sorting API)

현재 미션에서는 위 항목을 핵심 구현 범위로 확장하지 않는다.
