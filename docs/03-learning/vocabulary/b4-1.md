# B4-1 미션 용어 지도

**미션:** 나를 소개하는 웹페이지 처음부터 만들기  
**분야:** 웹 기초와 프론트엔드 (Web Basics & Frontend)  
**학습시간:** 80시간  
**표기 원칙:** 한글 용어 (English Term, 약어)  
**상태:** 1차 용어 목록 작성 완료 — 설명은 후속 학습 단계에서 추가

> 기준: B4-1 원본 Mission PDF의 미션 소개, 최종 결과물, 과제 목표, 기능 요구 사항, 보너스 과제, 개발 환경, 제약 사항, 결과 예시를 우선 반영한다. 원본에서 직접 요구하거나 구현·검증·평가에 필요한 용어를 중심으로 구성하며, 현재 단계에서는 용어 설명을 넣지 않는다.

---

## Level 0 — 선수 용어

- 웹 (Web)
- 웹 브라우저 (Web Browser)
- 프론트엔드 (Frontend)
- 웹페이지 (Web Page)
- 웹사이트 (Website)
- 하이퍼텍스트 마크업 언어 (HyperText Markup Language, HTML)
- 종속형 시트 (Cascading Style Sheets, CSS)
- 자바스크립트 (JavaScript, JS)
- 문서 객체 모델 (Document Object Model, DOM)
- 요소 (Element)
- 태그 (Tag)
- 속성 (Attribute)
- 클래스 (Class)
- 아이디 (ID)
- 선택자 (Selector)
- 이벤트 (Event)
- 상태 (State)
- 렌더링 (Rendering)
- 사용자 인터페이스 (User Interface, UI)
- 사용자 경험 (User Experience, UX)
- 반응형 웹 디자인 (Responsive Web Design, RWD)
- 모바일 퍼스트 (Mobile First)
- 개발자 도구 (Developer Tools, DevTools)
- 비주얼 스튜디오 코드 (Visual Studio Code, VS Code)
- 라이브 서버 (Live Server)
- 프로젝트 폴더 (Project Folder)
- 외부 스타일시트 (External Stylesheet)
- 외부 자바스크립트 파일 (External JavaScript File)
- 깃허브 저장소 (GitHub Repository)
- 깃허브 페이지 (GitHub Pages)

---

## Level 1 — 반드시 이해해야 하는 핵심 용어

- 시맨틱 HTML (Semantic HTML)
- 시맨틱 마크업 (Semantic Markup)
- 반응형 레이아웃 (Responsive Layout)
- 플렉스박스 (Flexible Box Layout, Flexbox)
- CSS 그리드 (CSS Grid Layout, Grid)
- 미디어 쿼리 (Media Query)
- 브레이크포인트 (Breakpoint)
- CSS 변수 / 사용자 정의 속성 (CSS Custom Property)
- 다크 모드 (Dark Mode)
- DOM 선택 (DOM Selection)
- DOM 조작 (DOM Manipulation)
- 이벤트 리스너 (Event Listener)
- 이벤트 처리 (Event Handling)
- 이벤트 → 상태 → 렌더링 흐름 (Event → State → Rendering Flow)
- 비동기 처리 (Asynchronous Processing)
- 비동기 함수 (Asynchronous Function)
- 애플리케이션 프로그래밍 인터페이스 (Application Programming Interface, API)
- GitHub API (GitHub API)
- 데이터 요청 (Data Request)
- 로딩 상태 (Loading State)
- 성공 상태 (Success State)
- 에러 상태 (Error State)
- 빈 상태 (Empty State)
- 폼 유효성 검사 (Form Validation)
- 로컬 스토리지 (Local Storage, localStorage)
- 접근성 대체 텍스트 (Alternative Text, alt)
- 레이트 리밋 (Rate Limit)

---

## Level 2 — 구현 필수 용어

### HTML 구조

- 헤더 요소 (`<header>`)
- 내비게이션 요소 (`<nav>`)
- 메인 요소 (`<main>`)
- 섹션 요소 (`<section>`)
- 아티클 요소 (`<article>`)
- 푸터 요소 (`<footer>`)
- 앵커 링크 (Anchor Link)
- 히어로 섹션 (Hero Section)
- 행동 유도 버튼 (Call To Action, CTA)
- About 섹션 (About Section)
- Skills 섹션 (Skills Section)
- Projects 섹션 (Projects Section)
- Contact 섹션 (Contact Section)
- Footer 섹션 (Footer Section)
- 이미지 대체 텍스트 (`alt` Attribute)
- 레이블 요소 (`<label>`)
- `for` 속성 (`for` Attribute)
- `id` 속성 (`id` Attribute)
- `for-id` 연결 (for-id Matching)
- `defer` 속성 (`defer` Attribute)

### CSS

- 루트 의사 클래스 (`:root` Pseudo-class)
- 데이터 속성 선택자 (`[data-theme="dark"]`)
- 색상 변수 (Color Variable)
- 폰트 변수 (Font Variable)
- 간격 변수 (Spacing Variable)
- `display: flex`
- `display: grid`
- `auto-fit`
- `minmax()`
- 768px 브레이크포인트 (768px Breakpoint)
- 1024px 브레이크포인트 (1024px Breakpoint)
- 호버 상태 (`:hover`)
- 전환 효과 (`transition`)
- 박스 그림자 (`box-shadow`)
- 햄버거 메뉴 (Hamburger Menu)

### JavaScript / DOM

- 상수 선언 (`const`)
- 블록 스코프 변수 선언 (`let`)
- `querySelector()`
- `querySelectorAll()`
- `textContent`
- `innerHTML`
- `classList`
- `classList.add()`
- `classList.remove()`
- `classList.toggle()`
- `addEventListener()`
- 클릭 이벤트 (`click` Event)
- 제출 이벤트 (`submit` Event)
- 스크롤 이벤트 (`scroll` Event)
- 입력 이벤트 (`input` Event)
- 기본 동작 방지 (`event.preventDefault()`)

### 인터랙션

- 활성 클래스 (`active` Class)
- 부드러운 스크롤 (Smooth Scroll)
- 스크롤 탑 버튼 (Scroll-to-Top Button)
- 스크롤 위치 (Scroll Position)
- 내비게이션 스타일 변경 (Navigation Style Change)
- 테마 토글 (Theme Toggle)
- 테마 상태 (Theme State)
- 스크롤 애니메이션 (Scroll Animation)
- 교차 관찰자 (Intersection Observer)
- 임계값 (Threshold)

### 폼 UX

- 필수값 검증 (Required Field Validation)
- 빈 필드 (Empty Field)
- 이메일 형식 검증 (Email Format Validation)
- 에러 메시지 (Error Message)
- 성공 메시지 (Success Message)

### ES6+ 문법

- ECMAScript 2015 이상 (ECMAScript 2015+, ES6+)
- 화살표 함수 (Arrow Function)
- 템플릿 리터럴 (Template Literal)
- 구조분해 할당 (Destructuring Assignment)
- 배열 메서드 (Array Method)
- `map()`
- `filter()`
- `forEach()`

### 비동기 / API

- `fetch()`
- `async`
- `await`
- `try/catch`
- API 엔드포인트 (API Endpoint)
- GitHub 사용자 저장소 엔드포인트 (`/users/{username}/repos`)
- 재시도 버튼 (Retry Button)
- 스피너 (Spinner)

### 배포 / 제출

- 배포 (Deployment)
- 배포 URL (Deployment URL)
- README (README)
- 프로젝트 설명 (Project Description)
- 사용 기술 (Technology Stack)
- 스크린샷 (Screenshot)
- 데스크톱 화면 (Desktop View)
- 모바일 화면 (Mobile View)
- 다크 모드 화면 (Dark Mode View)

---

## Level 3 — 구조·동작 원리 용어

- HTML 문서 구조 (HTML Document Structure)
- 시맨틱 구조 (Semantic Structure)
- 레이아웃 시스템 (Layout System)
- Flexbox 축 기반 레이아웃 (Flexbox Axis-based Layout)
- Grid 격자 기반 레이아웃 (Grid-based Layout)
- 모바일 퍼스트 설계 (Mobile-first Design)
- 반응형 전환 (Responsive Adaptation)
- DOM 요소 선택 흐름 (DOM Element Selection Flow)
- 이벤트 등록 흐름 (Event Registration Flow)
- 이벤트 발생 (Event Trigger)
- 상태 변경 (State Change)
- 화면 업데이트 (UI Update)
- 상태 기반 렌더링 (State-driven Rendering)
- 사용자 이벤트 → DOM 조작 → 화면 변화 (User Event → DOM Manipulation → UI Change)
- 사용자 이벤트 → 상태 변경 → 화면 업데이트 (User Event → State Change → UI Update)
- API 호출 흐름 (API Call Flow)
- 비동기 데이터 흐름 (Asynchronous Data Flow)
- 로딩 → 성공 / 에러 / 빈 상태 전환 (Loading → Success / Error / Empty Transition)
- 테마 상태 유지 (Theme State Persistence)
- 새로고침 후 상태 복원 (State Restoration After Refresh)
- 폼 입력 → 유효성 상태 → 메시지 렌더링 (Form Input → Validation State → Message Rendering)
- API 호출 → 상태 변경 → Projects 렌더링 (API Call → State Change → Projects Rendering)

---

## Level 4 — 검증·트러블슈팅 용어

- 브라우저 콘솔 (Browser Console)
- 개발자 도구 검사 (DevTools Inspection)
- DOM 요소 확인 (DOM Element Inspection)
- 이벤트 동작 확인 (Event Verification)
- 반응형 레이아웃 검증 (Responsive Layout Verification)
- 모바일 화면 검증 (Mobile View Verification)
- 태블릿 화면 검증 (Tablet View Verification)
- 데스크톱 화면 검증 (Desktop View Verification)
- 다크 모드 상태 유지 검증 (Dark Mode Persistence Verification)
- API 로딩 상태 검증 (API Loading State Verification)
- API 성공 상태 검증 (API Success State Verification)
- API 에러 상태 검증 (API Error State Verification)
- API 빈 상태 검증 (API Empty State Verification)
- 폼 필수값 검증 (Required Field Validation Test)
- 이메일 형식 검증 (Email Validation Test)
- GitHub API 레이트 리밋 (GitHub API Rate Limit)
- 시간당 60회 제한 (60 Requests per Hour Limit)
- HTTP 403 응답 (HTTP 403 Response)
- 재시도 (Retry)
- 배포 후 기능 검증 (Post-deployment Verification)
- 앵커 링크 검증 (Anchor Link Verification)
- 이미지 `alt` 검증 (Image alt Verification)
- `label-for-id` 연결 검증 (Label-for-id Verification)

---

## Level 5 — 평가에서 설명해야 하는 용어

- 시맨틱 태그 사용 이유 (Reason for Semantic Tags)
- HTML 구조 설계 기준 (HTML Structure Design Criteria)
- Flexbox와 Grid 비교 (Flexbox vs Grid)
- Flexbox 선택 기준 (Flexbox Selection Criteria)
- Grid 선택 기준 (Grid Selection Criteria)
- `querySelector()` DOM 선택 흐름 (querySelector DOM Selection Flow)
- `addEventListener()` 이벤트 연결 흐름 (addEventListener Event Binding Flow)
- 화살표 함수 사용 이유 (Reason for Arrow Functions)
- 구조분해 할당 사용 이유 (Reason for Destructuring)
- `map()` 사용 목적 (Purpose of map)
- `filter()` 사용 목적 (Purpose of filter)
- `fetch()` 비동기 요청 (fetch Asynchronous Request)
- `async/await` 비동기 흐름 (async/await Asynchronous Flow)
- 로딩/성공/실패 상태 표현 (Loading/Success/Failure State Rendering)
- 이벤트 → 상태 변경 → DOM 업데이트 (Event → State Change → DOM Update)
- React 상태-렌더링 흐름의 기초 (Foundation of React State-Rendering Flow)
- 로컬스토리지 상태 유지 (localStorage State Persistence)
- API 오류 처리 (API Error Handling)
- 폼 유효성 상태 관리 (Form Validation State Management)

---

## Advanced — 고도화 용어

> 아래 항목은 원본 Mission PDF의 보너스 과제 범위다.

- 프로젝트 언어별 필터링 (Project Filtering by Language)
- 타이핑 효과 / 타자기 효과 (Typing Effect / Typewriter Effect)
- Formspree (Formspree)
- EmailJS (EmailJS)
- 실제 이메일 전송 (Real Email Delivery)
- 시스템 다크 모드 감지 (System Dark Mode Detection)
- 선호 색상 스킴 (`prefers-color-scheme`)
- Font Awesome (Font Awesome)
- Google Fonts (Google Fonts)

---

## Top Core — 미션 시작 전에 반드시 이해해야 하는 용어

1. 하이퍼텍스트 마크업 언어 (HyperText Markup Language, HTML)
2. 종속형 시트 (Cascading Style Sheets, CSS)
3. 자바스크립트 (JavaScript, JS)
4. 문서 객체 모델 (Document Object Model, DOM)
5. 시맨틱 HTML (Semantic HTML)
6. 반응형 웹 디자인 (Responsive Web Design, RWD)
7. 모바일 퍼스트 (Mobile First)
8. 플렉스박스 (Flexible Box Layout, Flexbox)
9. CSS 그리드 (CSS Grid Layout, Grid)
10. 미디어 쿼리 (Media Query)
11. 브레이크포인트 (Breakpoint)
12. CSS 변수 / 사용자 정의 속성 (CSS Custom Property)
13. DOM 선택 (DOM Selection)
14. DOM 조작 (DOM Manipulation)
15. 이벤트 (Event)
16. 이벤트 리스너 (Event Listener)
17. `addEventListener()`
18. 상태 (State)
19. 렌더링 (Rendering)
20. 이벤트 → 상태 → 렌더링 흐름 (Event → State → Rendering Flow)
21. 로컬 스토리지 (Local Storage, localStorage)
22. 교차 관찰자 (Intersection Observer)
23. 폼 유효성 검사 (Form Validation)
24. 화살표 함수 (Arrow Function)
25. 템플릿 리터럴 (Template Literal)
26. 구조분해 할당 (Destructuring Assignment)
27. `map()`
28. `fetch()`
29. `async/await`
30. 애플리케이션 프로그래밍 인터페이스 (Application Programming Interface, API)
31. 로딩 상태 (Loading State)
32. 성공 상태 (Success State)
33. 에러 상태 (Error State)
34. 빈 상태 (Empty State)
35. GitHub Pages (GitHub Pages)

---

## 20 : 60 : 20 학습 배치

### 미션 전 20%

- HTML / CSS / JavaScript
- DOM
- 시맨틱 HTML
- 반응형 웹 디자인
- Flexbox / Grid
- 이벤트
- 상태
- 렌더링

### 미션 수행 중 60%

- HTML 시맨틱 태그
- CSS 변수 / 반응형 레이아웃
- DOM 선택 / 조작
- 이벤트 처리
- 햄버거 메뉴 / 스크롤 / 다크 모드
- Intersection Observer
- 폼 유효성 검사
- ES6+ 문법
- `fetch()` / `async/await`
- GitHub API 상태별 UI
- localStorage

### 미션 완료 후 20%

- 이벤트 → 상태 → 렌더링 구조 설명
- Flexbox와 Grid 선택 기준
- 비동기 데이터 흐름 설명
- 로딩/성공/에러/빈 상태 비교
- React 상태-렌더링 개념과 연결
- GitHub Pages 배포 검증

---

## B3-2 → B4-1 누적 학습 연결

### REVIEW

- 자료구조 (Data Structure)
- 알고리즘 (Algorithm)
- 명령줄 인터페이스 (Command-Line Interface, CLI)
- 입력 (Input)
- 출력 (Output)
- 상태 (State)
- 데이터 흐름 (Data Flow)

### APPLY

- 배열 / 리스트 순회 → `map()` / `filter()` / `forEach()`
- 상태 변화 → UI 상태 변화
- 입력 검증 → 폼 유효성 검사

### DEEPEN

- CLI 중심 프로그램 → 브라우저 기반 UI 프로그램
- 내부 데이터 처리 → DOM 렌더링
- 동기적 처리 중심 → 비동기 API 처리
- 결과 출력 → 상태 기반 화면 업데이트

---

## 다음 미션 연결

B4-1의 다음 학습 단계인 B4-2에서는 다음 개념이 직접 확장된다.

- DOM 조작 → React 컴포넌트 렌더링
- 이벤트 처리 → React 이벤트 처리
- 상태 → React State
- 상태 기반 렌더링 → React 재렌더링
- 비동기 API 호출 → React 데이터 패칭
- 반응형 UI → React SPA UI
